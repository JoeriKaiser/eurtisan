# Local observability stack environment
# Copy to .env.local and adjust before running `make obs-up`.

GRAFANA_ADMIN_PASSWORD=dev-admin-password
METRICS_TOKEN=dev-metrics-token
ALERTMANAGER_DEFAULT_RECEIVER=null
ALLOY_FARO_CORS_ORIGINS=http://localhost:3000
APP_CONTAINER_NAME=eurtisan-app
